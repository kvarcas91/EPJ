<UserControl x:Class="EPJ.Views.AddProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EPJ"
             mc:Ignorable="d"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="450" d:DesignWidth="800">


    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="AddProject" Grid.Column="0">
            <materialDesign:PackIcon Kind="Add" Height="24" Width="24"/>
        </Button>
        <ComboBox Grid.Column="0" Grid.Row="1" MinWidth="100"
          x:Name="Priorities" SelectedIndex="1" 
          SelectedItem="{Binding Path=Priority, Mode=OneWayToSource}"/>
        <materialDesign:PackIcon Kind="About" Height="24" Width="24"
                                 Grid.Column="0" Grid.Row="2"
                                 Foreground="Blue"/>
        <TextBox x:Name="Title" Grid.Row="2" Grid.Column="1"
                 materialDesign:HintAssist.IsFloating="True"
                 materialDesign:HintAssist.Hint="Project Title"/>

        <TextBox x:Name="Description" Grid.Row="1" Grid.Column="2"
                 />
        <DatePicker Grid.Row="1" Grid.Column="3"/>
        
    </Grid>
</UserControl>
